namespace ArrayRotation
{
    internal class Program
    {
        static void Main(string[] args)
        {
            while (true)
            {
                string Commands = Console.ReadLine();

                if (Commands == "Quit" || Commands == "quit" || Commands == "q" || Commands == "q")
                {
                    break;
                } else if (Commands == "Start" || Commands == "start" || Commands == "s" || Commands == "S") 
                {
                    try
                    {
                        
                    }
                }
            }
        }


        static int RotatingArray(List<int> A)
        {

            return A;
        }
    }
}
